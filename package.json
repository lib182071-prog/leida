{
  "name": "ai-content-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:local": "wrangler d1 execute content-db --local --file=schema.sql",
    "d1:remote": "wrangler d1 execute content-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
